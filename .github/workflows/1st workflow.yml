name: first workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: Ubuntu-latest
    steps:
      - name: print greeting
        run: echo "Hello World!"
      - name: get network config
        run: ifconfig -a
      - name: print bye
        run: echo "Done! Bye"
        
